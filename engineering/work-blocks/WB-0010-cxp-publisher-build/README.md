# WB-0010 — CXP Publisher Build

Implements deterministic `cybercore build` support for CXP/1 artifacts.

Delivered: canonical JSON, normalized tar metadata, Zstandard payload compression, SHA-256 identities, path/symlink checks and reproducibility tests.

Deferred: signing, encryption, registry publishing and Runtime CXP apply.
